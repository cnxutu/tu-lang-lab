[build-system]
requires = []
build-backend = "setuptools.build_meta"

[project]
name = "k5-python-pathlib-json-demo"
version = "0.1.0"
requires-python = ">=3.10"
description = "K5 Python standard library pathlib and json basics"
